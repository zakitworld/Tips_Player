<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:viewmodels="clr-namespace:Tips_Player.ViewModels"
             xmlns:helpers="clr-namespace:Tips_Player.Helpers"
             x:Class="Tips_Player.Views.FullscreenVideoPage"
             x:DataType="viewmodels:PlayerViewModel"
             BackgroundColor="Black"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False">

    <Grid>
        <!-- Video fills the entire screen -->
        <toolkit:MediaElement x:Name="VideoElement"
                              ShouldAutoPlay="False"
                              ShouldShowPlaybackControls="False"
                              Aspect="{Binding VideoAspect}"
                              HorizontalOptions="Fill"
                              VerticalOptions="Fill"
                              MediaOpened="OnMediaOpened"
                              PositionChanged="OnPositionChanged"/>

        <!-- Tap layer — single tap shows/hides controls, double tap exits -->
        <Grid BackgroundColor="Transparent"
              HorizontalOptions="Fill"
              VerticalOptions="Fill">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer NumberOfTapsRequired="1" Tapped="OnScreenTapped"/>
                <TapGestureRecognizer NumberOfTapsRequired="2" Tapped="OnScreenDoubleTapped"/>
            </Grid.GestureRecognizers>
        </Grid>

        <!-- Controls overlay (auto-hides) -->
        <Grid x:Name="ControlsOverlay" VerticalOptions="Fill" HorizontalOptions="Fill">

            <!-- Top bar: Back + Title + Aspect toggle -->
            <Grid VerticalOptions="Start"
                  BackgroundColor="#CC000000"
                  Padding="8,12,16,12"
                  ColumnDefinitions="56,*,90">

                <Button Text="{x:Static helpers:FontIcons.Close}"
                        FontFamily="FontAwesome"
                        Clicked="OnBackClicked"
                        BackgroundColor="Transparent"
                        TextColor="White"
                        FontSize="20"
                        HeightRequest="48"
                        WidthRequest="56"
                        Padding="0"/>

                <Label Grid.Column="1"
                       Text="{Binding CurrentMedia.Title, FallbackValue=''}"
                       TextColor="White"
                       FontSize="15"
                       FontAttributes="Bold"
                       VerticalOptions="Center"
                       LineBreakMode="TailTruncation"/>

                <Button Grid.Column="2"
                        Clicked="OnAspectToggled"
                        BackgroundColor="#60FFFFFF"
                        TextColor="Black"
                        FontSize="13"
                        HeightRequest="36"
                        CornerRadius="18"
                        Padding="0"
                        ToolTipProperties.Text="Toggle Aspect">
                    <Button.Triggers>
                        <DataTrigger TargetType="Button" Binding="{Binding VideoAspect}" Value="AspectFit">
                            <Setter Property="Text" Value="Fit" />
                        </DataTrigger>
                        <DataTrigger TargetType="Button" Binding="{Binding VideoAspect}" Value="AspectFill">
                            <Setter Property="Text" Value="Fill" />
                        </DataTrigger>
                    </Button.Triggers>
                </Button>
            </Grid>

            <!-- Bottom bar: seek bar + complete transport controls -->
            <Grid VerticalOptions="End"
                  BackgroundColor="#CC000000"
                  Padding="16,16,16,20"
                  RowDefinitions="Auto,Auto"
                  RowSpacing="8">

                <!-- Progress row -->
                <Grid ColumnDefinitions="52,*,52">
                    <Label Text="{Binding CurrentPositionFormatted}"
                           TextColor="White"
                           FontSize="12"
                           VerticalOptions="Center"/>
                    <Slider Grid.Column="1"
                            Minimum="0"
                            Maximum="{Binding SliderMaximum}"
                            Value="{Binding SliderPosition, Mode=TwoWay}"
                            MinimumTrackColor="{StaticResource AccentColor}"
                            MaximumTrackColor="#60FFFFFF"
                            ThumbColor="{StaticResource AccentColor}"
                            DragStarted="OnSliderDragStarted"
                            DragCompleted="OnSliderDragCompleted"
                            ValueChanged="OnSliderValueChanged"/>
                    <Label Grid.Column="2"
                           Text="{Binding DurationFormatted}"
                           TextColor="White"
                           FontSize="12"
                           HorizontalTextAlignment="End"
                           VerticalOptions="Center"/>
                </Grid>

                <!-- Buttons row -->
                <Grid Grid.Row="1" ColumnDefinitions="*,*,*,*,*,*,*,*,*"
                      ColumnSpacing="8" MaximumWidthRequest="820" HorizontalOptions="Center">
                    <Button Grid.Column="0" Text="{x:Static helpers:FontIcons.Shuffle}"
                            FontFamily="FontAwesome" Command="{Binding ToggleShuffleCommand}"
                            BackgroundColor="Transparent" TextColor="White" FontSize="18"
                            Opacity="{Binding IsShuffleEnabled, Converter={StaticResource BoolToOpacityConverter}}"
                            HeightRequest="48" WidthRequest="48" ToolTipProperties.Text="Shuffle"/>
                    <Button Text="{x:Static helpers:FontIcons.StepBackward}"
                            Grid.Column="1"
                            FontFamily="FontAwesome"
                            Command="{Binding PreviousCommand}"
                            BackgroundColor="Transparent"
                            TextColor="White"
                            FontSize="26"
                            HeightRequest="52"
                            WidthRequest="52"
                            ToolTipProperties.Text="Previous"/>

                    <Button Grid.Column="2" Text="{x:Static helpers:FontIcons.SeekBackward}"
                            FontFamily="FontAwesome" Command="{Binding SeekBackwardCommand}"
                            BackgroundColor="Transparent" TextColor="White" FontSize="20"
                            HeightRequest="48" WidthRequest="48" ToolTipProperties.Text="Back 10 seconds"/>

                    <Button Text="{Binding IsPlaying, Converter={StaticResource BoolToPlayPauseConverter}}"
                            Grid.Column="3"
                            FontFamily="FontAwesome"
                            Command="{Binding PlayPauseCommand}"
                            BackgroundColor="{StaticResource AccentColor}"
                            TextColor="White"
                            FontSize="30"
                            HeightRequest="64"
                            WidthRequest="64"
                            CornerRadius="32"
                            ToolTipProperties.Text="Play/Pause"/>

                    <Button Grid.Column="4" Text="{x:Static helpers:FontIcons.SeekForward}"
                            FontFamily="FontAwesome" Command="{Binding SeekForwardCommand}"
                            BackgroundColor="Transparent" TextColor="White" FontSize="20"
                            HeightRequest="48" WidthRequest="48" ToolTipProperties.Text="Forward 10 seconds"/>

                    <Button Text="{x:Static helpers:FontIcons.StepForward}"
                            Grid.Column="5"
                            FontFamily="FontAwesome"
                            Command="{Binding NextCommand}"
                            BackgroundColor="Transparent"
                            TextColor="White"
                            FontSize="26"
                            HeightRequest="52"
                            WidthRequest="52"
                            ToolTipProperties.Text="Next"/>

                    <Button Grid.Column="6" Text="{x:Static helpers:FontIcons.Repeat}"
                            FontFamily="FontAwesome" Command="{Binding ToggleRepeatCommand}"
                            BackgroundColor="Transparent" TextColor="White" FontSize="18"
                            Opacity="{Binding IsRepeatEnabled, Converter={StaticResource BoolToOpacityConverter}}"
                            HeightRequest="48" WidthRequest="48" ToolTipProperties.Text="Repeat"/>

                    <Button Grid.Column="7" Text="{Binding IsMuted, Converter={StaticResource BoolToMuteConverter}}"
                            FontFamily="FontAwesome" Command="{Binding ToggleMuteCommand}"
                            BackgroundColor="Transparent" TextColor="White" FontSize="18"
                            HeightRequest="48" WidthRequest="48" ToolTipProperties.Text="Mute or unmute"/>

                    <Button Grid.Column="8" Text="{x:Static helpers:FontIcons.Compress}"
                            FontFamily="FontAwesome" Clicked="OnBackClicked"
                            BackgroundColor="Transparent" TextColor="White" FontSize="18"
                            HeightRequest="48" WidthRequest="48" ToolTipProperties.Text="Exit fullscreen"/>
                </Grid>
            </Grid>
        </Grid>
    </Grid>

</ContentPage>
