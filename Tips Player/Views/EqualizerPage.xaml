<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Tips_Player.ViewModels"
             xmlns:models="clr-namespace:Tips_Player.Models"
             xmlns:helpers="clr-namespace:Tips_Player.Helpers"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:controls="clr-namespace:Tips_Player.Views.Controls"
             x:Class="Tips_Player.Views.EqualizerPage"
             x:DataType="viewmodels:EqualizerViewModel"
             Style="{StaticResource PlayerPageStyle}"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto,*">

        <!-- Header -->
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Padding="24,40,24,16">
            <HorizontalStackLayout Grid.Column="0" Spacing="12" VerticalOptions="Center">
                <Image Source="app_logo.png" HeightRequest="32" WidthRequest="32" VerticalOptions="Center"/>
                <Label Text="Equalizer" Style="{StaticResource PageTitleStyle}" VerticalOptions="Center"/>
            </HorizontalStackLayout>
            <HorizontalStackLayout Grid.Column="1" Spacing="8">
                <Label Text="Enabled" TextColor="{StaticResource TextSecondary}" VerticalOptions="Center" FontSize="14"/>
                <Switch IsToggled="{Binding IsEnabled}" OnColor="{StaticResource AccentColor}" VerticalOptions="Center"/>
            </HorizontalStackLayout>
        </Grid>

        <!-- Content -->
        <ScrollView Grid.Row="1" Padding="16,0,16,24">
            <VerticalStackLayout Spacing="16" Opacity="{Binding IsEnabled, Converter={StaticResource BoolToOpacityConverter}}">

                <!-- Presets Section -->
                <Border Background="{StaticResource CardBackground}" Stroke="Transparent" StrokeShape="RoundRectangle 12" Padding="16">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="Presets" FontSize="16" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"/>
                        <FlexLayout Wrap="Wrap" JustifyContent="Start" AlignItems="Start">
                            <BindableLayout.ItemsSource>
                                <x:Array Type="{x:Type models:EqualizerPreset}">
                                    <!-- Presets will be bound from ViewModel -->
                                </x:Array>
                            </BindableLayout.ItemsSource>
                        </FlexLayout>
                        <CollectionView ItemsSource="{Binding Presets}" HeightRequest="100" SelectionMode="None">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Horizontal" ItemSpacing="8"/>
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:EqualizerPreset">
                                    <Border Background="{StaticResource SurfaceBackground}"
                                           Stroke="Transparent"
                                           StrokeShape="RoundRectangle 8"
                                           Padding="12,8"
                                           WidthRequest="90">
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:EqualizerViewModel}}, Path=SelectPresetCommand}"
                                                                  CommandParameter="{Binding .}"/>
                                        </Border.GestureRecognizers>
                                        <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
                                            <Label Text="{Binding Icon}"
                                                   FontFamily="FontAwesome"
                                                   FontSize="20"
                                                   TextColor="{StaticResource AccentColor}"
                                                   HorizontalOptions="Center"/>
                                            <Label Text="{Binding Name}"
                                                   FontSize="11"
                                                   TextColor="{StaticResource TextPrimary}"
                                                   HorizontalTextAlignment="Center"
                                                   MaxLines="1"
                                                   LineBreakMode="TailTruncation"/>
                                        </VerticalStackLayout>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Border>

                <!-- 10-Band Equalizer -->
                <Border Background="{StaticResource CardBackground}" Stroke="Transparent" StrokeShape="RoundRectangle 12" Padding="16">
                    <VerticalStackLayout Spacing="16">
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Text="Equalizer Bands" FontSize="16" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"/>
                            <Label Grid.Column="1"
                                   Text="{Binding CurrentPreset.Name}"
                                   FontSize="14"
                                   TextColor="{StaticResource AccentColor}"/>
                        </Grid>

                        <!-- EQ Bands — VerticalSlider uses PanGestureRecognizer so touch works
                             correctly on Android (rotated Slider breaks Android gesture handling) -->
                        <Grid ColumnDefinitions="*,*,*,*,*,*,*,*,*,*" ColumnSpacing="4" HeightRequest="250">
                            <!-- Band 1: 32Hz -->
                            <Grid Grid.Column="0" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="2">
                                <Label Grid.Row="0" Text="{Binding Band32Hz, StringFormat='{0:+0.#;-0.#;0}'}"
                                       FontSize="9" TextColor="{StaticResource AccentColor}" HorizontalOptions="Center" FontAttributes="Bold"/>
                                <controls:VerticalSlider Grid.Row="1" Minimum="-12" Maximum="12" Value="{Binding Band32Hz}"
                                    ThumbColor="{StaticResource AccentColor}" VerticalOptions="Fill"
                                    ToolTipProperties.Text="32 Hz — Sub bass"/>
                                <Label Grid.Row="2" Text="32" FontSize="10" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center"/>
                                <Label Grid.Row="3" Text="Hz" FontSize="8" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center" Opacity="0.6"/>
                            </Grid>
                            <!-- Band 2: 64Hz -->
                            <Grid Grid.Column="1" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="2">
                                <Label Grid.Row="0" Text="{Binding Band64Hz, StringFormat='{0:+0.#;-0.#;0}'}"
                                       FontSize="9" TextColor="{StaticResource AccentColor}" HorizontalOptions="Center" FontAttributes="Bold"/>
                                <controls:VerticalSlider Grid.Row="1" Minimum="-12" Maximum="12" Value="{Binding Band64Hz}"
                                    ThumbColor="{StaticResource AccentColor}" VerticalOptions="Fill"
                                    ToolTipProperties.Text="64 Hz — Bass"/>
                                <Label Grid.Row="2" Text="64" FontSize="10" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center"/>
                                <Label Grid.Row="3" Text="Hz" FontSize="8" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center" Opacity="0.6"/>
                            </Grid>
                            <!-- Band 3: 125Hz -->
                            <Grid Grid.Column="2" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="2">
                                <Label Grid.Row="0" Text="{Binding Band125Hz, StringFormat='{0:+0.#;-0.#;0}'}"
                                       FontSize="9" TextColor="{StaticResource AccentColor}" HorizontalOptions="Center" FontAttributes="Bold"/>
                                <controls:VerticalSlider Grid.Row="1" Minimum="-12" Maximum="12" Value="{Binding Band125Hz}"
                                    ThumbColor="{StaticResource AccentColor}" VerticalOptions="Fill"
                                    ToolTipProperties.Text="125 Hz — Upper bass"/>
                                <Label Grid.Row="2" Text="125" FontSize="10" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center"/>
                                <Label Grid.Row="3" Text="Hz" FontSize="8" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center" Opacity="0.6"/>
                            </Grid>
                            <!-- Band 4: 250Hz -->
                            <Grid Grid.Column="3" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="2">
                                <Label Grid.Row="0" Text="{Binding Band250Hz, StringFormat='{0:+0.#;-0.#;0}'}"
                                       FontSize="9" TextColor="{StaticResource AccentColor}" HorizontalOptions="Center" FontAttributes="Bold"/>
                                <controls:VerticalSlider Grid.Row="1" Minimum="-12" Maximum="12" Value="{Binding Band250Hz}"
                                    ThumbColor="{StaticResource AccentColor}" VerticalOptions="Fill"
                                    ToolTipProperties.Text="250 Hz — Low midrange"/>
                                <Label Grid.Row="2" Text="250" FontSize="10" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center"/>
                                <Label Grid.Row="3" Text="Hz" FontSize="8" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center" Opacity="0.6"/>
                            </Grid>
                            <!-- Band 5: 500Hz -->
                            <Grid Grid.Column="4" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="2">
                                <Label Grid.Row="0" Text="{Binding Band500Hz, StringFormat='{0:+0.#;-0.#;0}'}"
                                       FontSize="9" TextColor="{StaticResource AccentColor}" HorizontalOptions="Center" FontAttributes="Bold"/>
                                <controls:VerticalSlider Grid.Row="1" Minimum="-12" Maximum="12" Value="{Binding Band500Hz}"
                                    ThumbColor="{StaticResource AccentColor}" VerticalOptions="Fill"
                                    ToolTipProperties.Text="500 Hz — Midrange"/>
                                <Label Grid.Row="2" Text="500" FontSize="10" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center"/>
                                <Label Grid.Row="3" Text="Hz" FontSize="8" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center" Opacity="0.6"/>
                            </Grid>
                            <!-- Band 6: 1kHz -->
                            <Grid Grid.Column="5" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="2">
                                <Label Grid.Row="0" Text="{Binding Band1kHz, StringFormat='{0:+0.#;-0.#;0}'}"
                                       FontSize="9" TextColor="{StaticResource AccentColor}" HorizontalOptions="Center" FontAttributes="Bold"/>
                                <controls:VerticalSlider Grid.Row="1" Minimum="-12" Maximum="12" Value="{Binding Band1kHz}"
                                    ThumbColor="{StaticResource AccentColor}" VerticalOptions="Fill"
                                    ToolTipProperties.Text="1 kHz — Upper midrange"/>
                                <Label Grid.Row="2" Text="1K" FontSize="10" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center"/>
                                <Label Grid.Row="3" Text="Hz" FontSize="8" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center" Opacity="0.6"/>
                            </Grid>
                            <!-- Band 7: 2kHz -->
                            <Grid Grid.Column="6" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="2">
                                <Label Grid.Row="0" Text="{Binding Band2kHz, StringFormat='{0:+0.#;-0.#;0}'}"
                                       FontSize="9" TextColor="{StaticResource AccentColor}" HorizontalOptions="Center" FontAttributes="Bold"/>
                                <controls:VerticalSlider Grid.Row="1" Minimum="-12" Maximum="12" Value="{Binding Band2kHz}"
                                    ThumbColor="{StaticResource AccentColor}" VerticalOptions="Fill"
                                    ToolTipProperties.Text="2 kHz — Presence"/>
                                <Label Grid.Row="2" Text="2K" FontSize="10" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center"/>
                                <Label Grid.Row="3" Text="Hz" FontSize="8" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center" Opacity="0.6"/>
                            </Grid>
                            <!-- Band 8: 4kHz -->
                            <Grid Grid.Column="7" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="2">
                                <Label Grid.Row="0" Text="{Binding Band4kHz, StringFormat='{0:+0.#;-0.#;0}'}"
                                       FontSize="9" TextColor="{StaticResource AccentColor}" HorizontalOptions="Center" FontAttributes="Bold"/>
                                <controls:VerticalSlider Grid.Row="1" Minimum="-12" Maximum="12" Value="{Binding Band4kHz}"
                                    ThumbColor="{StaticResource AccentColor}" VerticalOptions="Fill"
                                    ToolTipProperties.Text="4 kHz — Upper presence"/>
                                <Label Grid.Row="2" Text="4K" FontSize="10" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center"/>
                                <Label Grid.Row="3" Text="Hz" FontSize="8" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center" Opacity="0.6"/>
                            </Grid>
                            <!-- Band 9: 8kHz -->
                            <Grid Grid.Column="8" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="2">
                                <Label Grid.Row="0" Text="{Binding Band8kHz, StringFormat='{0:+0.#;-0.#;0}'}"
                                       FontSize="9" TextColor="{StaticResource AccentColor}" HorizontalOptions="Center" FontAttributes="Bold"/>
                                <controls:VerticalSlider Grid.Row="1" Minimum="-12" Maximum="12" Value="{Binding Band8kHz}"
                                    ThumbColor="{StaticResource AccentColor}" VerticalOptions="Fill"
                                    ToolTipProperties.Text="8 kHz — Brilliance"/>
                                <Label Grid.Row="2" Text="8K" FontSize="10" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center"/>
                                <Label Grid.Row="3" Text="Hz" FontSize="8" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center" Opacity="0.6"/>
                            </Grid>
                            <!-- Band 10: 16kHz -->
                            <Grid Grid.Column="9" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="2">
                                <Label Grid.Row="0" Text="{Binding Band16kHz, StringFormat='{0:+0.#;-0.#;0}'}"
                                       FontSize="9" TextColor="{StaticResource AccentColor}" HorizontalOptions="Center" FontAttributes="Bold"/>
                                <controls:VerticalSlider Grid.Row="1" Minimum="-12" Maximum="12" Value="{Binding Band16kHz}"
                                    ThumbColor="{StaticResource AccentColor}" VerticalOptions="Fill"
                                    ToolTipProperties.Text="16 kHz — Air / Sparkle"/>
                                <Label Grid.Row="2" Text="16K" FontSize="10" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center"/>
                                <Label Grid.Row="3" Text="Hz" FontSize="8" TextColor="{StaticResource TextMuted}" HorizontalOptions="Center" Opacity="0.6"/>
                            </Grid>
                        </Grid>

                        <!-- Buttons -->
                        <HorizontalStackLayout HorizontalOptions="Center" Spacing="12">
                            <Button Text="Reset" Command="{Binding ResetToFlatCommand}"
                                    BackgroundColor="{StaticResource SurfaceBackground}"
                                    TextColor="{StaticResource TextPrimary}"
                                    FontSize="14" HeightRequest="40" CornerRadius="8" Padding="16,0"/>
                            <Button Text="Save Preset" Command="{Binding ShowSavePresetDialogCommand}"
                                    BackgroundColor="{StaticResource AccentColor}"
                                    TextColor="{StaticResource TextPrimary}"
                                    FontSize="14" HeightRequest="40" CornerRadius="8" Padding="16,0"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <!-- Audio Effects -->
                <Border Background="{StaticResource CardBackground}" Stroke="Transparent" StrokeShape="RoundRectangle 12" Padding="16">
                    <VerticalStackLayout Spacing="16">
                        <Label Text="Audio Effects" FontSize="16" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"/>

                        <!-- Bass Boost -->
                        <Grid ColumnDefinitions="*,Auto,120">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="Bass Boost" TextColor="{StaticResource TextPrimary}" FontSize="14"/>
                                <Label Text="Enhance low frequencies" TextColor="{StaticResource TextMuted}" FontSize="12"/>
                            </VerticalStackLayout>
                            <Switch Grid.Column="1" IsToggled="{Binding BassBoostEnabled}" OnColor="{StaticResource AccentColor}" VerticalOptions="Center"/>
                            <Slider Grid.Column="2" Minimum="0" Maximum="100" Value="{Binding BassBoostStrength}"
                                    IsEnabled="{Binding BassBoostEnabled}"
                                    MinimumTrackColor="{StaticResource AccentColor}"
                                    MaximumTrackColor="{StaticResource Gray700}"
                                    ThumbColor="{StaticResource AccentColor}"/>
                        </Grid>

                        <!-- Virtualizer -->
                        <Grid ColumnDefinitions="*,Auto,120">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="Virtualizer" TextColor="{StaticResource TextPrimary}" FontSize="14"/>
                                <Label Text="Surround sound effect" TextColor="{StaticResource TextMuted}" FontSize="12"/>
                            </VerticalStackLayout>
                            <Switch Grid.Column="1" IsToggled="{Binding VirtualizerEnabled}" OnColor="{StaticResource AccentColor}" VerticalOptions="Center"/>
                            <Slider Grid.Column="2" Minimum="0" Maximum="100" Value="{Binding VirtualizerStrength}"
                                    IsEnabled="{Binding VirtualizerEnabled}"
                                    MinimumTrackColor="{StaticResource AccentColor}"
                                    MaximumTrackColor="{StaticResource Gray700}"
                                    ThumbColor="{StaticResource AccentColor}"/>
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <!-- Custom Presets -->
                <Border Background="{StaticResource CardBackground}" Stroke="Transparent" StrokeShape="RoundRectangle 12" Padding="16"
                       IsVisible="{Binding CustomPresets.Count, Converter={StaticResource IsNotNullConverter}}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="Custom Presets" FontSize="16" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"/>
                        <CollectionView ItemsSource="{Binding CustomPresets}" HeightRequest="60" SelectionMode="None">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Horizontal" ItemSpacing="8"/>
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:EqualizerPreset">
                                    <Border Background="{StaticResource SurfaceBackground}"
                                           Stroke="Transparent" StrokeShape="RoundRectangle 8" Padding="12,8">
                                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                            <Label Text="{Binding Name}" VerticalOptions="Center" TextColor="{StaticResource TextPrimary}">
                                                <Label.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:EqualizerViewModel}}, Path=SelectPresetCommand}"
                                                                          CommandParameter="{Binding .}"/>
                                                </Label.GestureRecognizers>
                                            </Label>
                                            <Button Grid.Column="1" Text="&#xf00d;" FontFamily="FontAwesome" FontSize="12"
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:EqualizerViewModel}}, Path=DeleteCustomPresetCommand}"
                                                    CommandParameter="{Binding .}"
                                                    BackgroundColor="Transparent" TextColor="{StaticResource TextMuted}"
                                                    HeightRequest="24" WidthRequest="24" Padding="0"
                                                    ToolTipProperties.Text="Delete Preset"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Save Preset Dialog Overlay -->
        <Grid Grid.RowSpan="2" IsVisible="{Binding IsCustomPresetDialogVisible}"
              BackgroundColor="#80000000">
            <Border Background="{StaticResource CardBackground}"
                   StrokeShape="RoundRectangle 16" Stroke="Transparent" Padding="24"
                   VerticalOptions="Center" HorizontalOptions="Center"
                   WidthRequest="300">
                <VerticalStackLayout Spacing="16">
                    <Label Text="Save Custom Preset" FontSize="18" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"/>
                    <Entry Text="{Binding NewPresetName}" Placeholder="Preset name"
                           PlaceholderColor="{StaticResource TextMuted}"
                           TextColor="{StaticResource TextPrimary}"
                           BackgroundColor="{StaticResource SurfaceBackground}"/>
                    <HorizontalStackLayout HorizontalOptions="End" Spacing="12">
                        <Button Text="Cancel" Command="{Binding HideSavePresetDialogCommand}"
                                BackgroundColor="{StaticResource SurfaceBackground}"
                                TextColor="{StaticResource TextPrimary}" CornerRadius="8"/>
                        <Button Text="Save" Command="{Binding SaveCustomPresetCommand}"
                                BackgroundColor="{StaticResource AccentColor}"
                                TextColor="{StaticResource TextPrimary}" CornerRadius="8"/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>
        </Grid>

    </Grid>

</ContentPage>
