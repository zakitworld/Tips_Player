<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:viewmodels="clr-namespace:Tips_Player.ViewModels"
             xmlns:helpers="clr-namespace:Tips_Player.Helpers"
             xmlns:controls="clr-namespace:Tips_Player.Views.Controls"
             x:Class="Tips_Player.Views.PlayerPage"
             x:DataType="viewmodels:PlayerViewModel"
             BackgroundColor="{StaticResource PageBackground}"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="True">
    
    <ContentPage.Resources>
        <x:Int32 x:Key="SleepTimer0">0</x:Int32>
        <x:Int32 x:Key="SleepTimer15">15</x:Int32>
        <x:Int32 x:Key="SleepTimer30">30</x:Int32>
        <x:Int32 x:Key="SleepTimer60">60</x:Int32>
    </ContentPage.Resources>

    <Grid x:Name="RootGrid" BackgroundColor="{StaticResource PageBackground}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="DisplayStates">
                <VisualState x:Name="Mobile">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter TargetName="NormalModeContentGrid" Property="ColumnDefinitions" Value="*" />
                        <Setter TargetName="NormalModeContentGrid" Property="RowDefinitions" Value="Auto,Auto" />
                        <Setter TargetName="NormalModeContentGrid" Property="WidthRequest" Value="-1" />
                        <Setter TargetName="NormalModeContentGrid" Property="HorizontalOptions" Value="Fill" />
                        <Setter TargetName="NormalMediaContainer" Property="Grid.Column" Value="0" />
                        <Setter TargetName="NormalMediaContainer" Property="Grid.Row" Value="0" />
                        <Setter TargetName="ControlsPanel" Property="Grid.Column" Value="0" />
                        <Setter TargetName="ControlsPanel" Property="Grid.Row" Value="1" />
                        <Setter TargetName="NormalMediaContainer" Property="HeightRequest" Value="320" />
                        <Setter TargetName="NormalMediaContainer" Property="WidthRequest" Value="-1" />
                        <Setter TargetName="NormalMediaContainer" Property="VerticalOptions" Value="Fill" />
                        <Setter TargetName="ControlsPanel" Property="WidthRequest" Value="-1" />
                        <Setter TargetName="ControlsPanel" Property="Margin" Value="0,10,0,0" />
                        <Setter TargetName="ControlsPanel" Property="VerticalOptions" Value="Fill" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Desktop">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1150" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter TargetName="NormalModeContentGrid" Property="ColumnDefinitions" Value="520,580" />
                        <Setter TargetName="NormalModeContentGrid" Property="RowDefinitions" Value="*" />
                        <Setter TargetName="NormalModeContentGrid" Property="WidthRequest" Value="1132" />
                        <Setter TargetName="NormalModeContentGrid" Property="HorizontalOptions" Value="Center" />
                        <Setter TargetName="NormalMediaContainer" Property="Grid.Column" Value="0" />
                        <Setter TargetName="NormalMediaContainer" Property="Grid.Row" Value="0" />
                        <Setter TargetName="ControlsPanel" Property="Grid.Column" Value="1" />
                        <Setter TargetName="ControlsPanel" Property="Grid.Row" Value="0" />
                        <Setter TargetName="NormalMediaContainer" Property="HeightRequest" Value="520" />
                        <Setter TargetName="NormalMediaContainer" Property="WidthRequest" Value="520" />
                        <Setter TargetName="NormalMediaContainer" Property="VerticalOptions" Value="Start" />
                        <Setter TargetName="ControlsPanel" Property="WidthRequest" Value="580" />
                        <Setter TargetName="ControlsPanel" Property="Margin" Value="32,0,0,0" />
                        <Setter TargetName="ControlsPanel" Property="VerticalOptions" Value="Start" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!-- Normal Mode Layout (fullscreen is handled by FullscreenVideoPage modal) -->
        <Grid RowDefinitions="Auto,*">

            <!-- Header -->
            <Grid Grid.Row="0"
                  ColumnDefinitions="*,Auto"
                  Padding="{OnPlatform Android='8,8', Default='24,16'}"
                  BackgroundColor="{StaticResource PageBackground}">
                <VerticalStackLayout Grid.Column="0" Spacing="1" VerticalOptions="Center">
                    <Label Text="PLAYBACK" Style="{StaticResource EyebrowStyle}" IsVisible="{OnPlatform Android=False, Default=True}"/>
                    <Label Text="{Binding PlayerHeading}"
                           FontSize="{OnPlatform Android=19, Default=28}"
                           FontAttributes="Bold"
                           TextColor="{StaticResource TextPrimary}"/>
                </VerticalStackLayout>
                <HorizontalStackLayout Grid.Column="1" Spacing="{OnPlatform Android=2, Default=6}">
                    <!-- Queue Button -->
                    <Button Text="{x:Static helpers:FontIcons.List}"
                            FontFamily="FontAwesome"
                            Command="{Binding NavigateToQueueCommand}"
                            BackgroundColor="{StaticResource ElevatedBackground}"
                            TextColor="{StaticResource TextPrimary}"
                            FontSize="{OnPlatform Android=14, Default=16}"
                            HeightRequest="{OnPlatform Android=34, Default=40}"
                            WidthRequest="{OnPlatform Android=34, Default=40}"
                            CornerRadius="{OnPlatform Android=10, Default=14}"
                            Padding="0"
                            AutomationProperties.Name="Open queue"
                            ToolTipProperties.Text="Queue"/>
                    <!-- Lyrics Button -->
                    <Button Text="{x:Static helpers:FontIcons.Font}"
                            FontFamily="FontAwesome"
                            Command="{Binding NavigateToLyricsCommand}"
                            BackgroundColor="{StaticResource ElevatedBackground}"
                            TextColor="{StaticResource TextPrimary}"
                            FontSize="{OnPlatform Android=14, Default=16}"
                            HeightRequest="{OnPlatform Android=34, Default=40}"
                            WidthRequest="{OnPlatform Android=34, Default=40}"
                            CornerRadius="{OnPlatform Android=10, Default=14}"
                            Padding="0"
                            AutomationProperties.Name="Open lyrics"
                            ToolTipProperties.Text="Lyrics"/>
                    <!-- Equalizer Button -->
                    <Button Text="{x:Static helpers:FontIcons.Equalizer}"
                            FontFamily="FontAwesome"
                            Command="{Binding NavigateToEqualizerCommand}"
                            BackgroundColor="{StaticResource ElevatedBackground}"
                            TextColor="{StaticResource TextPrimary}"
                            FontSize="{OnPlatform Android=14, Default=16}"
                            HeightRequest="{OnPlatform Android=34, Default=40}"
                            WidthRequest="{OnPlatform Android=34, Default=40}"
                            CornerRadius="{OnPlatform Android=10, Default=14}"
                            Padding="0"
                            AutomationProperties.Name="Open equalizer"
                            ToolTipProperties.Text="Equalizer"/>
                    <!-- Car Mode Button -->
                    <Button Text="{x:Static helpers:FontIcons.Car}"
                            FontFamily="FontAwesome"
                            Command="{Binding EnterCarModeCommand}"
                            BackgroundColor="{StaticResource ElevatedBackground}"
                            TextColor="{StaticResource TextPrimary}"
                            FontSize="{OnPlatform Android=14, Default=16}"
                            HeightRequest="{OnPlatform Android=34, Default=40}"
                            WidthRequest="{OnPlatform Android=34, Default=40}"
                            CornerRadius="{OnPlatform Android=10, Default=14}"
                            Padding="0"
                            AutomationProperties.Name="Enter car mode"
                            ToolTipProperties.Text="Car Mode"/>
                    <!-- Add Files Button -->
                    <Button Text="{x:Static helpers:FontIcons.Plus}"
                            FontFamily="FontAwesome"
                            Command="{Binding AddFilesCommand}"
                            BackgroundColor="{StaticResource ElevatedBackground}"
                            TextColor="{StaticResource TextPrimary}"
                            FontSize="{OnPlatform Android=14, Default=18}"
                            HeightRequest="{OnPlatform Android=34, Default=40}"
                            WidthRequest="{OnPlatform Android=34, Default=40}"
                            CornerRadius="{OnPlatform Android=10, Default=14}"
                            Padding="0"
                            AutomationProperties.Name="Add media files"
                            ToolTipProperties.Text="Add media"/>
                </HorizontalStackLayout>
            </Grid>

            <!-- Main Content Area -->
            <ScrollView Grid.Row="1">
                <Grid x:Name="NormalModeContentGrid" Padding="{OnPlatform Android='12,0,12,20', Default='0,0,0,28'}">
                    
                    <!-- Media Display Area -->
                    <Border x:Name="NormalMediaContainer"
                            StrokeThickness="0"
                            BackgroundColor="{StaticResource CardBackground}"
                            StrokeShape="RoundRectangle 24"
                            HorizontalOptions="Fill">
                        <Border.Shadow>
                            <Shadow Brush="#000000" Offset="0,8" Radius="24" Opacity="0.5"/>
                        </Border.Shadow>

                        <Grid>
                            <toolkit:MediaElement x:Name="MediaElement"
                                                  ShouldAutoPlay="False"
                                                  ShouldShowPlaybackControls="False"
                                                  Aspect="{Binding VideoAspect}"
                                                  MediaOpened="OnMediaOpened"
                                                  PositionChanged="OnPositionChanged"
                                                  IsVisible="{Binding ShowVideoPlayer}"
                                                  HorizontalOptions="Fill"
                                                  VerticalOptions="Fill"
                                                  ZIndex="10"/>

                            <HorizontalStackLayout x:Name="NormalVideoControls"
                                                   HorizontalOptions="End"
                                                   VerticalOptions="Start"
                                                   Margin="12"
                                                   Spacing="8"
                                                   ZIndex="20"
                                                   IsVisible="False">
                                <Button Command="{Binding ToggleAspectCommand}" BackgroundColor="#99000000"
                                        TextColor="White" FontSize="13" HeightRequest="40" WidthRequest="60"
                                        CornerRadius="20" Padding="0" ToolTipProperties.Text="Fit or fill video">
                                    <Button.Triggers>
                                        <DataTrigger TargetType="Button" Binding="{Binding VideoAspect}" Value="AspectFit"><Setter Property="Text" Value="Fit" /></DataTrigger>
                                        <DataTrigger TargetType="Button" Binding="{Binding VideoAspect}" Value="AspectFill"><Setter Property="Text" Value="Fill" /></DataTrigger>
                                    </Button.Triggers>
                                </Button>
                                <Button Text="{x:Static helpers:FontIcons.Expand}" FontFamily="FontAwesome"
                                        Command="{Binding ToggleFullScreenCommand}" BackgroundColor="#99000000"
                                        TextColor="White" FontSize="18" HeightRequest="40" WidthRequest="40"
                                        CornerRadius="20" Padding="0" ToolTipProperties.Text="Fullscreen"/>
                            </HorizontalStackLayout>
                            <Grid.GestureRecognizers>
                                <!-- Double-tap → fullscreen -->
                                <TapGestureRecognizer NumberOfTapsRequired="2" Command="{Binding ToggleFullScreenCommand}"/>
                                <!-- Swipe left/right → next/previous -->
                                <PanGestureRecognizer PanUpdated="OnAlbumArtPanned"/>
                            </Grid.GestureRecognizers>

                            <!-- Video placeholder - actual MediaElement is above -->
                            <BoxView IsVisible="{Binding ShowVideoPlayer}"
                                     Color="Transparent"/>

                            <!-- Audio Visualization / Album Art -->
                            <Grid IsVisible="{Binding ShowVideoPlayer, Converter={StaticResource InverseBoolConverter}}"
                                  BackgroundColor="{StaticResource SurfaceBackground}">

                                <!-- Gradient Background -->
                                <BoxView>
                                    <BoxView.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#171A2A" Offset="0.0"/>
                                            <GradientStop Color="#222345" Offset="0.52"/>
                                            <GradientStop Color="#123C45" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </BoxView.Background>
                                </BoxView>

                                <Image Source="{Binding CurrentMedia.AlbumArtPath}"
                                       Aspect="AspectFill"
                                       IsVisible="{Binding CurrentMedia.AlbumArtPath, Converter={StaticResource IsNotNullConverter}}"/>

                                <!-- Music Icon with Glow Effect -->
                                <Grid VerticalOptions="Center" HorizontalOptions="Center">
                                    <Ellipse WidthRequest="180"
                                             HeightRequest="180"
                                             Fill="#1a6366F1"
                                             HorizontalOptions="Center"
                                             VerticalOptions="Center"/>
                                    <Ellipse WidthRequest="140"
                                             HeightRequest="140"
                                             Fill="#336366F1"
                                             HorizontalOptions="Center"
                                             VerticalOptions="Center"/>

                                    <Label Text="{x:Static helpers:FontIcons.Music}"
                                           FontFamily="FontAwesome"
                                           FontSize="80"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center"
                                           IsVisible="{Binding CurrentMedia, Converter={StaticResource IsNotNullConverter}}"/>

                                    <VerticalStackLayout IsVisible="{Binding CurrentMedia, Converter={StaticResource IsNotNullConverter}, ConverterParameter=Invert}"
                                                         VerticalOptions="Center"
                                                         HorizontalOptions="Center"
                                                         Spacing="8">
                                        <Label Text="{x:Static helpers:FontIcons.Music}"
                                               FontFamily="FontAwesome"
                                               FontSize="60"
                                               HorizontalOptions="Center"/>
                                        <Label Text="Select a track"
                                               TextColor="{StaticResource TextSecondary}"
                                               FontSize="14"
                                               HorizontalOptions="Center"/>
                                    </VerticalStackLayout>
                                </Grid>

                                <!-- Sound wave indicator — bottom-centre of art area -->
                                <controls:SoundWaveIndicator
                                    IsVisible="{Binding CurrentMedia, Converter={StaticResource IsNotNullConverter}}"
                                    IsPlaying="{Binding IsPlaying}"
                                    BarColor="{StaticResource AccentColor}"
                                    HorizontalOptions="Center"
                                    VerticalOptions="End"
                                    Scale="2.5"
                                    Margin="0,0,0,20"/>
                            </Grid>

                        </Grid>
                    </Border>

                    <!-- Interaction Controls Panel -->
                    <VerticalStackLayout x:Name="ControlsPanel" Spacing="10">
                        
                        <!-- Track Info -->
                        <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
                            <Label Text="{Binding CurrentMedia.Title, FallbackValue='Choose something to play'}"
                                   FontSize="24"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource TextPrimary}"
                                   HorizontalTextAlignment="Center"
                                   MaxLines="1"
                                   LineBreakMode="TailTruncation"/>
                            <Label Text="{Binding CurrentMedia.DisplaySubtitle, FallbackValue='Your next favorite track is waiting'}"
                                   FontSize="14"
                                   TextColor="{StaticResource TextSecondary}"
                                   HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>

                        <!-- Progress Section -->
                        <Grid RowDefinitions="Auto,Auto" Margin="0,8,0,0">
                            <Slider x:Name="ProgressSlider"
                                    Minimum="0"
                                    Maximum="{Binding SliderMaximum}"
                                    Value="{Binding SliderPosition, Mode=TwoWay}"
                                    MinimumTrackColor="{StaticResource AccentColor}"
                                    MaximumTrackColor="{StaticResource Gray700}"
                                    ThumbColor="{StaticResource AccentColor}"
                                    DragStarted="OnSliderDragStarted"
                                    DragCompleted="OnSliderDragCompleted"
                                    ValueChanged="OnSliderValueChanged"/>

                            <Grid Grid.Row="1" ColumnDefinitions="*,*" Margin="0,2,0,0">
                                <Label Grid.Column="0"
                                       Text="{Binding CurrentPositionFormatted}"
                                       FontSize="11"
                                       TextColor="{StaticResource TextMuted}"/>
                                <Label Grid.Column="1"
                                       Text="{Binding DurationFormatted}"
                                       FontSize="11"
                                       TextColor="{StaticResource TextMuted}"
                                       HorizontalTextAlignment="End"/>
                            </Grid>
                        </Grid>

                        <!-- Playback Controls -->
                        <HorizontalStackLayout HorizontalOptions="Center"
                                               Spacing="{OnPlatform Android=2, Default=12}"
                                               Margin="0,4,0,0">
                            <Button Text="{x:Static helpers:FontIcons.Shuffle}"
                                    Command="{Binding ToggleShuffleCommand}"
                                    Style="{StaticResource SecondaryControlStyle}"
                                    FontSize="14" Opacity="{Binding IsShuffleEnabled, Converter={StaticResource BoolToOpacityConverter}}"
                                    ToolTipProperties.Text="Shuffle"/>

                            <Button Text="{x:Static helpers:FontIcons.StepBackward}"
                                    Command="{Binding PreviousCommand}"
                                    Style="{StaticResource ControlButtonStyle}"
                                    HeightRequest="44" WidthRequest="44" FontSize="18"
                                    ToolTipProperties.Text="Previous"/>

                            <Button Text="{x:Static helpers:FontIcons.SeekBackward}"
                                    Command="{Binding SeekBackwardCommand}"
                                    Style="{StaticResource SecondaryControlStyle}"
                                    FontSize="14"
                                    AutomationProperties.Name="Rewind 10 seconds"
                                    ToolTipProperties.Text="Back 10 seconds"/>

                            <Button Text="{Binding IsPlaying, Converter={StaticResource BoolToPlayPauseConverter}}"
                                    FontFamily="FontAwesome"
                                    Command="{Binding PlayPauseCommand}"
                                    Style="{StaticResource PlayButtonStyle}"
                                    HeightRequest="64"
                                    WidthRequest="64"
                                    CornerRadius="22"
                                    AutomationProperties.Name="Play or pause"
                                    ToolTipProperties.Text="Play/Pause">
                                <Button.Shadow>
                                    <Shadow Brush="{StaticResource AccentColor}" Offset="0,4" Radius="12" Opacity="0.4"/>
                                </Button.Shadow>
                            </Button>

                            <Button Text="{x:Static helpers:FontIcons.SeekForward}"
                                    Command="{Binding SeekForwardCommand}"
                                    Style="{StaticResource SecondaryControlStyle}"
                                    FontSize="14"
                                    AutomationProperties.Name="Fast forward 10 seconds"
                                    ToolTipProperties.Text="Forward 10 seconds"/>

                            <Button Text="{x:Static helpers:FontIcons.StepBackward}"
                                    Command="{Binding NextCommand}"
                                    Style="{StaticResource ControlButtonStyle}"
                                    HeightRequest="44" WidthRequest="44" FontSize="18"
                                    Rotation="180"
                                    ToolTipProperties.Text="Next"/>

                            <Button Text="{x:Static helpers:FontIcons.Repeat}"
                                    Command="{Binding ToggleRepeatCommand}"
                                    Style="{StaticResource SecondaryControlStyle}"
                                    FontSize="14" Opacity="{Binding IsRepeatEnabled, Converter={StaticResource BoolToOpacityConverter}}"
                                    ToolTipProperties.Text="Repeat"/>
                        </HorizontalStackLayout>

                        <!-- Volume Control -->
                        <Grid ColumnDefinitions="Auto,*,Auto" Margin="12,8,12,0">
                            <Button Grid.Column="0"
                                    Text="{Binding IsMuted, Converter={StaticResource BoolToMuteConverter}}"
                                    FontFamily="FontAwesome"
                                    Command="{Binding ToggleMuteCommand}"
                                    BackgroundColor="Transparent"
                                    TextColor="{StaticResource TextSecondary}"
                                    FontSize="18"
                                    HeightRequest="36"
                                    WidthRequest="36"
                                    CornerRadius="18"
                                    ToolTipProperties.Text="Mute/Unmute"/>

                            <Slider Grid.Column="1"
                                    Minimum="0"
                                    Maximum="1"
                                    Value="{Binding Volume}"
                                    MinimumTrackColor="{StaticResource AccentColor}"
                                    MaximumTrackColor="{StaticResource Gray700}"
                                    ThumbColor="{StaticResource AccentColor}"
                                    Margin="4,0"/>

                            <Label Grid.Column="2"
                                   Text="{x:Static helpers:FontIcons.VolumeUp}"
                                   FontFamily="FontAwesome"
                                   FontSize="16"
                                   TextColor="{StaticResource TextSecondary}"
                                   VerticalOptions="Center"/>
                        </Grid>

                        <!-- Sleep Timer -->
                        <VerticalStackLayout Spacing="6" Margin="0,8,0,0">
                            <HorizontalStackLayout HorizontalOptions="Center" Spacing="8">
                                <Label Text="Sleep Timer:"
                                       TextColor="{StaticResource TextMuted}"
                                       VerticalOptions="Center"
                                       FontSize="11"/>
                                <Button Text="Off"
                                        Command="{Binding SetSleepTimerCommand}"
                                        CommandParameter="{StaticResource SleepTimer0}"
                                        Style="{StaticResource SecondaryControlStyle}"
                                        WidthRequest="36" FontSize="10" Padding="0"/>
                                <Button Text="15m"
                                        Command="{Binding SetSleepTimerCommand}"
                                        CommandParameter="{StaticResource SleepTimer15}"
                                        Style="{StaticResource SecondaryControlStyle}"
                                        WidthRequest="36" FontSize="10" Padding="0"/>
                                <Button Text="30m"
                                        Command="{Binding SetSleepTimerCommand}"
                                        CommandParameter="{StaticResource SleepTimer30}"
                                        Style="{StaticResource SecondaryControlStyle}"
                                        WidthRequest="36" FontSize="10" Padding="0"/>
                                <Button Text="60m"
                                        Command="{Binding SetSleepTimerCommand}"
                                        CommandParameter="{StaticResource SleepTimer60}"
                                        Style="{StaticResource SecondaryControlStyle}"
                                        WidthRequest="36" FontSize="10" Padding="0"/>
                                <Button Text="Custom"
                                        Command="{Binding ToggleCustomTimerCommand}"
                                        Style="{StaticResource SecondaryControlStyle}"
                                        WidthRequest="50" FontSize="10" Padding="0"/>
                            </HorizontalStackLayout>

                            <!-- Custom Timer Input -->
                            <Grid IsVisible="{Binding IsCustomTimerVisible}"
                                  HorizontalOptions="Center"
                                  ColumnDefinitions="Auto,80,Auto"
                                  ColumnSpacing="8">
                                <Label Grid.Column="0"
                                       Text="Minutes:"
                                       TextColor="{StaticResource TextMuted}"
                                       FontSize="12"
                                       VerticalOptions="Center"/>
                                <Entry Grid.Column="1"
                                       Text="{Binding CustomTimerMinutes}"
                                       Keyboard="Numeric"
                                       Placeholder="e.g. 45"
                                       PlaceholderColor="{StaticResource TextMuted}"
                                       TextColor="{StaticResource TextPrimary}"
                                       BackgroundColor="{StaticResource CardBackground}"
                                       FontSize="14"
                                       HorizontalTextAlignment="Center"
                                       ReturnCommand="{Binding SetCustomTimerCommand}"/>
                                <Button Grid.Column="2"
                                        Text="Set"
                                        Command="{Binding SetCustomTimerCommand}"
                                        BackgroundColor="{StaticResource AccentColor}"
                                        TextColor="{StaticResource TextPrimary}"
                                        FontSize="12"
                                        HeightRequest="36"
                                        WidthRequest="50"
                                        CornerRadius="8"
                                        Padding="0"/>
                            </Grid>

                            <Label Text="{Binding SleepTimerMinutes, StringFormat='Timer active: {0}m remaining'}"
                                   IsVisible="{Binding IsSleepTimerActive}"
                                   HorizontalOptions="Center"
                                   TextColor="{StaticResource AccentColor}"
                                   FontSize="11"
                                   FontAttributes="Italic"/>
                        </VerticalStackLayout>

                        <!-- Playback Speed Control -->
                        <Border Background="{StaticResource CardBackground}" Stroke="Transparent"
                               StrokeShape="RoundRectangle 8" Padding="12" Margin="0,8,0,0">
                            <Grid ColumnDefinitions="Auto,*,Auto,Auto,Auto" ColumnSpacing="8">
                                <Label Grid.Column="0" Text="Speed:"
                                       TextColor="{StaticResource TextSecondary}" FontSize="12" VerticalOptions="Center"/>
                                <Label Grid.Column="1" Text="{Binding PlaybackSpeedText}"
                                       TextColor="{StaticResource AccentColor}" FontSize="14"
                                       FontAttributes="Bold" VerticalOptions="Center" HorizontalOptions="Center"/>
                                <Button Grid.Column="2" Text="&#xf068;" FontFamily="FontAwesome"
                                        Command="{Binding DecreaseSpeedCommand}"
                                        BackgroundColor="{StaticResource SurfaceBackground}"
                                        TextColor="{StaticResource TextPrimary}"
                                        FontSize="12" HeightRequest="32" WidthRequest="32" CornerRadius="16" Padding="0"
                                        ToolTipProperties.Text="Decrease Speed"/>
                                <Button Grid.Column="3" Text="1x"
                                        Command="{Binding ResetSpeedCommand}"
                                        BackgroundColor="{StaticResource SurfaceBackground}"
                                        TextColor="{StaticResource TextPrimary}"
                                        FontSize="11" HeightRequest="32" WidthRequest="40" CornerRadius="8" Padding="0"
                                        ToolTipProperties.Text="Reset Speed"/>
                                <Button Grid.Column="4" Text="&#xf067;" FontFamily="FontAwesome"
                                        Command="{Binding IncreaseSpeedCommand}"
                                        BackgroundColor="{StaticResource SurfaceBackground}"
                                        TextColor="{StaticResource TextPrimary}"
                                        FontSize="12" HeightRequest="32" WidthRequest="32" CornerRadius="16" Padding="0"
                                        ToolTipProperties.Text="Increase Speed"/>
                            </Grid>
                        </Border>

                        <!-- A-B Repeat Control -->
                        <Border Background="{StaticResource CardBackground}" Stroke="Transparent"
                               StrokeShape="RoundRectangle 8" Padding="12" Margin="0,4,0,0">
                            <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="8">
                                <Label Grid.Column="0" Text="A-B:"
                                       TextColor="{StaticResource TextSecondary}" FontSize="12" VerticalOptions="Center"/>
                                <Label Grid.Column="1" Text="{Binding ABRepeatStatus}"
                                       TextColor="{StaticResource TextPrimary}" FontSize="12"
                                       VerticalOptions="Center" HorizontalOptions="Center"/>
                                <Button Grid.Column="2" Text="A⟷B"
                                        Command="{Binding ToggleABRepeatCommand}"
                                        BackgroundColor="{Binding IsABRepeatEnabled, Converter={StaticResource BoolToColorConverter}}"
                                        TextColor="{StaticResource TextPrimary}"
                                        FontSize="10" HeightRequest="32" WidthRequest="50" CornerRadius="8" Padding="0"
                                        ToolTipProperties.Text="Set A-B Loop Points"/>
                                <Button Grid.Column="3" Text="&#xf00d;" FontFamily="FontAwesome"
                                        Command="{Binding ClearABRepeatCommand}"
                                        BackgroundColor="{StaticResource SurfaceBackground}"
                                        TextColor="{StaticResource TextPrimary}"
                                        FontSize="12" HeightRequest="32" WidthRequest="32" CornerRadius="16" Padding="0"
                                        IsVisible="{Binding RepeatPointA, Converter={StaticResource IsNotNullConverter}}"
                                        ToolTipProperties.Text="Clear A-B Loop"/>
                            </Grid>
                        </Border>

                    </VerticalStackLayout>
                </Grid>
            </ScrollView>
        </Grid>

    </Grid>

</ContentPage>
