<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:allowBackup="true"
                 android:icon="@mipmap/appicon"
                 android:roundIcon="@mipmap/appicon_round"
                 android:supportsRtl="true">

        <!-- Foreground media playback service (keep alive when app is backgrounded) -->
        <service android:name="com.companyname.tipsplayer.MediaPlaybackService"
                 android:foregroundServiceType="mediaPlayback"
                 android:exported="false"/>

        <!-- Broadcast receiver for notification play/pause/skip actions -->
        <receiver android:name="com.companyname.tipsplayer.MediaActionReceiver"
                  android:exported="false">
            <intent-filter>
                <action android:name="com.companyname.tipsplayer.PLAY_PAUSE"/>
                <action android:name="com.companyname.tipsplayer.NEXT"/>
                <action android:name="com.companyname.tipsplayer.PREV"/>
                <action android:name="com.companyname.tipsplayer.STOP"/>
            </intent-filter>
        </receiver>

    </application>

    <!-- Network -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Storage (≤ Android 12) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
                     android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="29"/>

    <!-- Granular media permissions (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+: must declare the exact foreground service type -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

</manifest>
